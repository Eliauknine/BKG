 private void visitPHPDocType(TypeReference typeReference, ProblemSeverity severi{  if (idx == -1) { // look if complete name is valid identifier name  if (PHPTextSequenceUtilities.readNamespaceStartIndex(name, name.length(), false) == 0) {  visit(typeReference, severity); 			} 		} else if (idx > 0) {  // look if name part before "::" is valid (and non-empty)  // identifier name  if (PHPTextSequenceUtilities.readNamespaceStartIndex(name, idx, false) == 0) {  visit(new TypeReference(typeReference.start(), typeReference.start() + idx, name.substring(0, idx)),  severity); 			} 		} 	} 
