 public void run(){  commitThread1.start(); commitThread2.start();  sleep(2000);  // do another commit.  CDOTransaction transaction4 = session2.openTransaction();  Company company4 = transaction4.getObject(company1);  company4.setName("company2");  transaction4.commit();  // check if update arrived.  transaction1.waitForUpdate(transaction4.getLastCommitTime(), DEFAULT_TIMEOUT_EXPECTED);  assertEquals(company4.getName(), company1.getName());  // check committing on the other session too.  CDOTransaction transaction5 = session1.openTransaction();  Company company5 = transaction5.getObject(company1);  company5.setName("company3");  transaction5.commit();  // check if update arrived.  transaction2.waitForUpdate(transaction5.getLastCommitTime(), DEFAULT_TIMEOUT_EXPECTED);  assertEquals(company5.getName(), company2.getName());   } 
