     } private void doOpen() {  Recommendation<ISnippet> recommendation = cast(selection.getValue());  IWorkbenchPage page = PlatformUI.getWorkbench().getActiveWorkbenchWindow().getActivePage();  try {  ISnippet snippet = recommendation.getProposal();  ISnippetRepository repository = findRepoForOriginalSnippet(snippet);  final SnippetEditorInput input = new SnippetEditorInput(snippet, repository);  page.openEditor(input, "org.eclipse.recommenders.snipmatch.rcp.editors.snippet");          } catch (Exception e) {  Throwables.propagate(e);         }     }
