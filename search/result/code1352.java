  filePath += "manifest.yml";  }  fs.readFile(filePath, "utf8", function(err, fileContent){  if(err){  return reject({"code":404, "message": err.message});  }  fulfill(fileContent);  });  }).then(function(fileContent){  var manifest = yaml.safeLoad(fileContent);  var manifestAST = yamlAstParser.load(fileContent);  transformManifest(manifest);  // TODO when meet the case where need to do symbolResolver (as in JAVA) then implement.  analyzeManifest(manifest, manifestAST, fileContent)  .then(function(){  setDefaultManifestProperties(req,manifest);  return manifest;  });  }); }
