 return super.computeTargetAuto(link);    }  if (isMapsLink(link)) {  //Open the link in the same browser window to open the maps app. Otherwise the popup gets blocked without notice.  return UrlTarget.SELF;     }  return super.computeTargetAuto(link);   }  public boolean isMapsLink(String link) {  if ((StringUtility.find(link, "http://maps.google.com") >= 0) || (StringUtility.find(link, "https://maps.google.com") >= 0)) {  return true;     
