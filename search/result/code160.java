 private CDOCommitInfo makeCommitInfo(long timeStamp, long previousTimeStamp){  return commitInfoManager.createCommitInfo(branch, timeStamp, previousTimeStamp, userID, comment, mergeSource, commitData);    }  private CDOLockChangeInfo makeUnlockChangeInfo(CommitTransactionResult result)     {  Map<CDOObject, CDOLockState> lockStates = getLockStates();  if (lockStates.isEmpty()) } 
