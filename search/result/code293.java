 public void export(IFile typeFile, String destination, boolean forceOverwrite, L{  if (!forceOverwrite && filesExisted) {  // create message dialog to ask about merging if forceOverwrite is not set  String msg = "Overwrite " + type.getName() + ".cpp" + " and " + type.getName() + ".h. " 						+ ((opener != null) 								? "\nMerge will create backup of the original File and open an editor to merge the files manually!" 								: "");   MessageDialog msgDiag = new MessageDialog(Display.getDefault().getActiveShell(), "File Exists", null,  msg, MessageDialog.QUESTION_WITH_CANCEL, buttonLabels, 0); } } 
