  if (menuManager.isEmpty()) { IDifferenceFilter.Registry registry = EMFCompareRCPUIPlugin.getDefault() 					.getFilterActionRegistry();  for (IDifferenceFilter filterProvider : registry.getFilters(scope, comparison)) {  FilterAction action = new FilterAction(filterProvider.getLabel(), structureMergeViewerFilter,  filterProvider);  if (filterProvider.defaultSelected()) {  action.setChecked(true);  action.run(); 				}  menuManager.add(action); 			} 		} 	}
