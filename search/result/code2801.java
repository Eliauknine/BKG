  Message result = message; if (messageList.size() > 1) {  PopupMenu popupMenu = new PopupMenu(messageList, labelProvider);  boolean b = popupMenu.show(Display.getCurrent().getActiveShell());  if (b) {  result = (Message) popupMenu.getResult(); 				} 			}  if (result == message) {  // the new one  definitions.getRootElements().add(message);  message.setId(null);  ModelUtil.setID(message);  message.setName(oldName); 			} else {  // and existing one  message = result; 			}  putBusinessObject(context, message);  return message; 		}
