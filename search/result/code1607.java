	 * @return <code>true</code> when package is registered	 */  public static boolean containsPackage(final String candidate) {  if (getPackages().containsKey(candidate))  return true;  int lastDot = candidate.lastIndexOf('.');  if (lastDot > 0) {  Collection<String> packageList = getPackages().get(candidate.substring(0, lastDot));  if (packageList != null)  return packageList.contains(candidate.substring(lastDot + 1)); 		}  return false; 	} 
