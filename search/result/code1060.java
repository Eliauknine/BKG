 private IStatus run(IMarketplaceService marketplaceService, IProgressMonitor mon{  ISearchResult searchResult = marketplaceService.tagged(fileExtensionTag, monitor); nodes = searchResult.getNodes(); 		} catch (CoreException ex) {  return new Status(IStatus.ERROR,  MarketplaceClientUiPlugin.getInstance().getBundle().getSymbolicName(), ex.getMessage(), ex); 		}  if (nodes.isEmpty()) {  return Status.OK_STATUS; } 
