 public void execute(InternalCDOObject object, CDOState state, CDOEvent event, In{  // Compute revision delta and register it with the tx  CDORevision cleanRevision = revisionManager.getRevisionByVersion(id, revKey, -1, true);  CDORevisionDelta revisionDelta = revision.compare(cleanRevision);  if (!revisionDelta.isEmpty())       {  transaction.registerRevisionDelta(revisionDelta);  transaction.registerDirty(object, (CDOFeatureDelta)null);  changeState(object, CDOState.DIRTY);       }  else       {  changeState(object, CDOState.CLEAN);       }  // Add the object to the set of reattached objects } 
