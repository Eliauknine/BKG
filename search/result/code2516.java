 private void updateConnectionIfNeeded(DataAssociation association, ItemAwareElem{  df.delete(dc);				} 			}  else if (pe!=null){  // There is no existing DataAssociation connection, but the newly selected source or target  // is some kind of data object shape, so we need to create connection between the Activity  // (or Throw/Catch Event) that owns the DataAssociation, and the new data object shape.  Point p = GraphicsUtil.createPoint((AnchorContainer) pe);  Anchor ownerAnchor = AnchorUtil.findNearestAnchor(owner, p);  p = GraphicsUtil.createPoint(owner);  Anchor peAnchor = AnchorUtil.findNearestAnchor((AnchorContainer) pe, p);  AddConnectionContext ac = null;  if (association instanceof DataOutputAssociation) {  ac = new AddConnectionContext(ownerAnchor, peAnchor); } 
