  ISearchResult searchResult = marketplaceService.tagged(fileExtensionTag, monitor); nodes = searchResult.getNodes(); 		} catch (CoreException ex) {  IStatus status = new Status(IStatus.ERROR,  MarketplaceClientUiPlugin.getInstance().getBundle().getSymbolicName(), ex.getMessage(), ex);  MarketplaceClientUiPlugin.getInstance().getLog().log(status);  // Do not return this status as it would show an error  return Status.CANCEL_STATUS; 		}
