 public Iterable<RestrictionVerdict> getRestrictions(Iterable<String> featureIden{  List<RestrictionVerdict> result = new ArrayList<>(); for (String featureId : featureIdentifiers) {  List<RestrictionVerdict> found = restrictions.computeIfAbsent(featureId, list -> new ArrayList<>());  if (found.isEmpty()) {   Iterable<RestrictionVerdict> examined = examineFeaturePermissons(featureId, configuration);   examined.forEach(found::add);  }  found.forEach(result::add); 		}  return result; } 
