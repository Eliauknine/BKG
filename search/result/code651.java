 private void processAttributeMappings(Map<String, MarkerGroupingEntry> entryIDsT{  for (IConfigurationElement mapping : mappings) { String entryId = mapping.getAttribute(MARKER_GROUPING_ENTRY);  if (entryIDsToEntries.containsKey(entryId)) {   MarkerGroupingEntry entry = entryIDsToEntries.get(entryId);  entry.getMarkerGroup().mapAttribute(attributeGrouping, entry, mapping.getAttribute(VALUE)); 				} else {  IDEWorkbenchPlugin.log(NLS.bind( } } } 
