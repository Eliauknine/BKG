 public void execute(File file, IProgressMonitor progressMonitor) throws IOExcept{  // sanity check  if (directContents.size() != 1 && !(directContents.get(0) instanceof ChangePackage)) {  throw new IOException("File is corrupt, does not contain changes."); 		}  final ChangePackage changePackage = (ChangePackage) directContents.get(0);  // / TODO  // if (!projectSpace.isInitialized()) {  // projectSpace.init();  // }  projectSpace.applyOperations(changePackage.getOperations(), true); 	} 
