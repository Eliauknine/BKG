 private void createWrappers(final Object instance, final String identifier, fina{ 			}		}  // create wrappers for static fields  if (!reload) {  // this is only done upon initial loading as we try to create constants here  for (final Field field : ModuleHelper.getFields(instance.getClass())) {  try {  // only wrap if field is not already declared  if (!getScriptEngine().hasVariable(codeFactory.getSaveVariableName(field.getName()))) {  final String code = codeFactory.createStaticFieldWrapper(this, identifier, field);  if ((code != null) && !code.isEmpty()) {  scriptCode.append(code); } } } } 
