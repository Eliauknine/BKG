 		if (!PHPFlags.isStatic(enclosingMethod.getFlags())) { 			IType declaringType = enclosingMethod.getDeclaringType(); 			if (declaringType != null) { 				if ("$this".startsWith(prefix)) {  					reporter.reportField( 							new FakeField((ModelElement) declaringType, 									"$this", 0, 0), suffix, replaceRange, false, ICompletionReporter.RELEVANCE_ADJUST); //NON-NLS-1  				} 			} 		} }
