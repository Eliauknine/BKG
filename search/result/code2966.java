         }      }  catch (IOException e) {  e.printStackTrace();       }  finally {  srcStream.close(); //        if (m_file != null) { //          try { //            m_file.delete(); //            File parentPath = new File(m_file.getParent()); //            if (parentPath.isDirectory() && parentPath.listFiles().length == 0) { //              parentPath.delete(); //            } //          } //          catch (Exception e) { //            // NOP //          } //        }       }     }
