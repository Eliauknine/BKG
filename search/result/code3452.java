 void prePerform(MultiStatus status, EngineSession session, IProgressMonitor moni{  private void mainPerform(MultiStatus status, EngineSession session, Operand[] operands, SubMonitor subMonitor) {  IProfile profile = session.getProfile();  subMonitor.beginTask("", operands.length);   for (int i = 0; i < operands.length; i++) {  subMonitor.setWorkRemaining(operands.length - i);  if (subMonitor.isCanceled()) } } } 
