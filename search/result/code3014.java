 private Message getMessage(String text, Position curr, InterfaceItem item, boole{  private ArrayList<String> getArgs(String text, Position curr) { proceedToToken(text, curr);  if (text.charAt(curr.pos)!='(')  return null; 		++curr.pos; } } 
