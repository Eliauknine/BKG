 #endif const char scEscapedCharacters[] = { '"', '\'', '&', '<', '>'};  const char* const scReplacementForEscapedCharacters[sizeof(scEscapedCharacters)] = {"&quot;", "&apos;", "&amp;",  "&lt;", "&gt;"};  /**        * Gets the extra space needed in string for escaped chars.        * @param paString The null ended C-string that's being checked for chars that need to be escaped        * @return Extra number of chars needed to allocated the chars once they are escaped  */  size_t getExtraSizeForEscapedChars(const char* const paString);  /**        * Transforms string with escaped chars (&apos;) into string with the original chars ('). }
