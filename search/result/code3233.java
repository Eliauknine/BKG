 private boolean preparePhpRegions(Collection<StyleRange> holdResults,{  styleLength -= (partitionStartOffset - styleStart); styleStart = partitionStartOffset; 					}  if (styleStart + styleLength > partitionStartOffset 							+ partitionLength) {// if the region ends after the  // requested end position -  // making it shorter  styleLength -= (styleStart + styleLength) 								- (partitionStartOffset + partitionLength); 					} 
