 module.exports.gitFileDecorator = function(rootName, req, filepath, originalJson{  return clone.getRepoByPath(filepath, req.user.workspaceDir) .then(function(repo) {  if(repo){  var fileDir = api.join(rootName, repo.workdir().substring(req.user.workspaceDir.length + 1) , "/");  return Promise.resolve(getBranch(repo))  .then(function(branchname){  addGitLinks(result,branchname,fileDir); } } } } 
