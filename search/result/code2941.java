  // make sure that parent mappings are initialized?  if (isChildDescriptor()) {  getInheritancePolicy().getParentDescriptor().initialize(session);  getCachePolicy().initializeFromParent(getInheritancePolicy().getParentDescriptor().getCachePolicy(), this,  getInheritancePolicy().getParentDescriptor(), session);  // Setup this early before useOptimisticLocking is called so that subclass  // versioned by superclass are also covered  getInheritancePolicy().initializeOptimisticLocking();  // EL bug 336486  getInheritancePolicy().initializeCacheInvalidationPolicy();         }  // Mappings must be sorted before field are collected in the order of the mapping for indexes to work. }
