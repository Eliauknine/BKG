	} public void start(Event event, Object target) {  if (timeline == null) {  timeline = createTimelineGroup(findTarget(target));  updateTimeline(timeline, target);  AnimationManager.getInstance().addTimeline(timeline); 		}  if (event.type == XWTMaps.getEvent("swt.dispose")) {  AnimationManager.getInstance().play(timeline, false);  event.doit = true; 		} else {  AnimationManager.getInstance().play(timeline, false); 		} 	}  public void stop(Event event) {  AnimationManager.getInstance().stop(timeline); 	}  public void pause(Event event) {  AnimationManager.getInstance().pause(timeline); 	}  public void resume(Event event) {  AnimationManager.getInstance().resume(timeline); 	}  public void playReverse(Event event) {  AnimationManager.getInstance().playReverse(timeline); 	
