 private void processExtension(IExtensionTracker tracker, IExtension extension,{  if (element.getName().equals(MARKER_GROUPING_ENTRY)) { MarkerGroupingEntry entry = new MarkerGroupingEntry(element);  String groupName = element.getAttribute(MARKER_GROUPING);  Collection<MarkerGroupingEntry> entries;  if (groupIDsToEntries.containsKey(groupName)) {  entries = groupIDsToEntries.get(groupName); 				} else {  entries = new HashSet<>(); 				} } } 
