 public Object adjustReference(Object value, EStructuralFeature feature, int inde{       { // First lock all objects (incl. possible ref targets).  // This is transient operation, it does not check for existance!  lockManager.lock(LockType.WRITE, transaction, lockedObjects, 1000);  // If all locks could be acquired, check if locked targets do still exist  if (lockedTargets != null) } } 
