  if (managedNotifications.contains(current)) {  int currentEvent = current.getEventType();    // filtering notification concerning creation of ITreeItemAxis created as children  if (Notification.ADD == currentEvent) {  current = getAddNotification(current, managedNotifications, index);  // check if the remove event is move action 				}else if (Notification.REMOVE_MANY == currentEvent || Notification.REMOVE == currentEvent && notificationsIterator.hasNext()) {  current = getMoveNotification(current, managedNotifications, index); 				} } }
