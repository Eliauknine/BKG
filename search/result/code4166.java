 			ProjectParsingManager projectParsingManager = new ProjectParsingManager(project);			project2ParsingManagerMap.put(project, projectParsingManager); 			PHPProjectModel projectModel = null; 			if (project != null && project.isAccessible()) { 				register2RemoveManagerWhenProjectCloses(project); 				projectModel = PHPWorkspaceModelManager.getInstance().getModelForProject(project); 			} else { } }
