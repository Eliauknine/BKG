  return null;		}  if (item.getParentItem() == null) {  // Key navigation is only available for child items ATM  return null; 		}  GalleryItem group = item.getParentItem();  switch (key) {  case SWT.HOME:  return getFirstItem(group, START);  case SWT.END:  return getFirstItem(group, END); 		}  int pos = group.indexOf(item);  GalleryItem next = null;  if (gallery.isVertical()) {  int hCount = ((Integer) group.getData(H_COUNT)).intValue();  int maxVisibleRows = gallery.getClientArea().height / itemHeight;  switch (key) {  case SWT.ARROW_LEFT:  next = goLeft(group, pos); } }
