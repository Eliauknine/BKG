 public CDOID getResourceID(String path){  CDOID folderID = null; if (CDOURIUtil.SEGMENT_SEPARATOR.equals(path))     {  folderID = getResourceID(null, null);     }  else     {  List<String> names = CDOURIUtil.analyzePath(path);  for (String name : names)       {  folderID = getResourceID(folderID, name);       }     }  return folderID;   }  /**    * @retrn never <code>null</code>    */  private CDOID getResourceID(CDOID folderID, String name)   {  folderID = getResourceNodeID(folderID, name);  if (folderID == null) } 
