  /**    * Optionally return comparator which is applicable to all objects that getMatchID    * may return. Its behavior on other objects has no consequences.    * If present, it is used to alter the performance of the matching phase by using    * TreeMaps instead of HashMaps (log(n) time cost for the main operations but no    * issue related to HashMap's capacity or load factor).    * @return comparator or null    * @see java.util.TreeMap    */  Comparator<?> getMatchIDComparator();  /**    * Return whether match ID information must be maintained for better traceability    * but at the price of larger memory footprint    * @see IPureMatch#getMatchID()     */  boolean keepMatchIDs(); }
