 public void performNodeQuery(IProgressMonitor monitor, Set<? extends INode> node{  result.setNodes(new ArrayList<Node>()); if (!monitor.isCanceled()) {  if (!nodes.isEmpty()) {  SubMonitor nodesProgress = SubMonitor.convert(progress.newChild(500), nodes.size());  for (INode node : nodes) {  node = marketplaceService.getNode(node, nodesProgress.newChild(1));  result.getNodes().add((Node) node); 					} 				} else { } } } 
