  AbstractPagePart pagePart = (AbstractPagePart) activePart;  String pageIndexParameter = (String) parameters.get(PAGE_INDEX_PARAM);  if (pageIndexParameter != null) {  // switch to the page set in the given page parameter  int index = Ints.tryParse(pageIndexParameter);  int indexOf = pagePart.getPages().indexOf(pagePart.getCurrentPage());  element.setChecked(indexOf == index);             }         }
