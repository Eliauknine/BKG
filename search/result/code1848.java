 public int render(Workbook workbook, VTableControl vElement, final ViewModelCont{ 				} if (vElement.getDetailEditing() != DetailEditing.NONE) {  EObject tableEntry;  if (observableList.size() > i) {  tableEntry = (EObject) observableList.get(i); 					} else {  tableEntry = EcoreUtil.create(EReference.class.cast(observableList.getElementType()) 							.getEReferenceType()); 					}  final VView viewModel = getView(vElement, tableEntry, viewModelContext);  final ViewModelContext subViewModelContext = new EMFFormsSpreadsheetViewModelContext(viewModel, } 
