  prefs.writePrefs(allPrefs); }  function updateLastOpendTabsPrefs(tabs, originalWorkspace){  var allPrefs = prefs.readPrefs();  var openedTabs = allPrefs.user && allPrefs.user.workspace && allPrefs.user.workspace.openedTabs;  if(!openedTabs){  ((allPrefs.user || (allPrefs.user = {})).workspace || (allPrefs.user.workspace = {})).openedTabs || (allPrefs.user.workspace.openedTabs={});  }  var currentWorkspace = (originalWorkspace ? originalWorkspace : allPrefs.user.workspace.currentWorkspace) || configParams.workspace;  allPrefs.user.workspace.openedTabs[currentWorkspace] = tabs;  prefs.writePrefs(allPrefs);  }
