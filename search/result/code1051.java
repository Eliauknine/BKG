 		if (deprecatedTag != null) 			handleDeprecatedTag(deprecatedTag); 		if (shortDescription != null && shortDescription.length() > 0) 			fBuf.append(shortDescription); 		if (longDescription != null && longDescription.length() > 0) { 			fBuf.append("<p>");  			longDescription = longDescription.replaceAll("(\r\n){2,}|\n{2,}|\r{2,}", "</p><p>");   			fBuf.append(longDescription); 			fBuf.append("</p>");  		} else if (fMethod != null) { 			CharSequence inherited = fJavadocLookup.getInheritedMainDescription(fMethod); 			handleInherited(inherited); 		} }
