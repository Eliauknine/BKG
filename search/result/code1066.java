 private static void setUpResourceSet(ResourceSet resourceSet, IWorkspaceRoot roo{  uri = URI.createURI("platform:/plugin/" + v); 		}  // System.err.println(uri);  try {  // prevent some unnecessary calls by checking the uri  if (resourceSet.getURIConverter().exists(uri, null)) {  resourceSet.getResource(uri, true); 			} 		} catch (final Exception e) {  e.printStackTrace();  // System.err.println("=============> Failing"); 		} 
