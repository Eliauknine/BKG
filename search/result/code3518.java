 int width = WordUtil.convertTo( table.getWidth( ), context				.getCurrentWidth( ) );  width = Math.min( width, context.getCurrentWidth( ) );  wordWriter.startTable( table.getComputedStyle( ), width );  int[] cols = computeTblColumnWidths( table, width );  wordWriter.writeColumn( cols );  context.addTable( cols, table.getComputedStyle( ) ); 	}  public void startTableBand( ITableBandContent band ) 	{ 	
