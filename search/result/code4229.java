 	 * excluding php closing tag (?>) and comments   	 */ 	protected boolean shouldAddClosingBracket(final IStructuredDocument document, final int offset, final boolean isQuote) throws BadLocationException { 		if (document.getLength() == offset) 			return true; 		final char currChar = document.getChar(offset); 		if (Character.isWhitespace(currChar) || isClosingBracket(currChar) || isQuote && isQuote(currChar) || currChar == ';') } }
