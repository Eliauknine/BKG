 private boolean preChecks(CDORevision revision, CDORevisionProvider revisionProv{  private BasicEList<Matcher> getMatchers()   {  if (matchers != null)     {  return matchers;     }  String path = getPath();  PatternStyle patternStyle = getPatternStyle();  boolean includeParents = isIncludeParents();  if (userTokenPos == USER_TOKEN_UNINITIALIZED)     {  userTokenPos = path.indexOf(USER_TOKEN);     } } } 
