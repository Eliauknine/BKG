      * @return true if the given element's label matches the filter text     */  protected boolean isLeafMatch(Viewer viewer, Object element) {  String labelText = ((ILabelProvider) ((StructuredViewer) viewer).getLabelProvider()).getText(element);  if (labelText == null) {  // also check for CellLabelProvider, which are also ILabelProvider, e.g.,  // ColumnLabelProvider  CellLabelProvider labelProvider = null;  if (viewer instanceof TreeViewer) {  labelProvider = ((TreeViewer) viewer).getLabelProvider(0);             } else if (viewer instanceof TableViewer) {  labelProvider = ((TableViewer) viewer).getLabelProvider(0);             }  if (labelProvider instanceof ILabelProvider) {  labelText = ((ILabelProvider) labelProvider).getText(element);             }         }  return wordMatches(labelText);     }  /**      * Take the given filter text and break it down into words using BreakIterator.      * }
