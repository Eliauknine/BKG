 		}		programName = programName.toLowerCase(); 		processAttributes.put(IProcess.ATTR_PROCESS_TYPE, programName); 		IProgressMonitor subMonitor = new SubProgressMonitor(monitor, 10); 		if (p != null) { 			subMonitor = new SubProgressMonitor(monitor, 80); 			process = DebugPlugin.newProcess(launch, p, phpExe.toOSString(), processAttributes); 			if (process == null) { 				p.destroy(); } }
