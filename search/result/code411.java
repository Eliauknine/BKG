     addressToParse = parser[0];  }  CParameterParser pathParser(addressToParse, '/', 2); //IP:PORT/PATH?PARAMETERS  if(2 == pathParser.parseParameters()) {  mPath = strchr(addressToParse, '/');     CParameterParser portParser(pathParser[0], ':', 2);  if(2 == portParser.parseParameters()) {  mHost = portParser[0];  mPort = static_cast<TForteUInt16>(forte::core::util::strtoul(portParser[1], 0, 10));     } else {  mHost = pathParser[0];  DEVLOG_INFO("[HTTP Layer] No port was found on the parameter, using default 80\n");     }   } else { }
