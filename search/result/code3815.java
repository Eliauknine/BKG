 public void execute(InternalCDOObject object, CDOState state, CDOEvent event, Object NULL)    {  CDOViewImpl view = (CDOViewImpl)object.cdoView();  if (object instanceof CDOResourceImpl)       {  view.getResourceSet().getResources().remove(object);       }  view.deregisterObject(object);  object.cdoInternalSetState(CDOState.INVALID);  // Need information of the objects, do not set them to NULL  // (object).cdoInternalSetID(null);  // (object).cdoInternalSetRevision(null);  // (object).cdoInternalSetView(null);     }   
