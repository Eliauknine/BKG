  Node node = createInterfacenode(umlViewProvider, container, interfaceRequired);  setPositionNode(node, position);  String kind = "REQUIRED";  createEdge(umlViewProvider, node, kind); 		}  for (Object currentprovided : providedInterface) {  EObject interfaceProvided = null;  if (currentprovided instanceof NewElementRepresentation) {  interfaceProvided = ((NewElementRepresentation) currentprovided).getEObject(); 			} else { } }
