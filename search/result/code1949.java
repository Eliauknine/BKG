 @Override public void run() {  // toggle filter  FilteredTree filteredTree = this.page.getPageControl();  filteredTree.setShowFilterControls(!filteredTree.isShowFilterControls());  // if the filter has become visible set the focus on it  // if the filter has disappeared clear the filter text to display all nodes  if (filteredTree.isShowFilterControls()) {  filteredTree.getFilterControl().setFocus();         } else {  filteredTree.getFilterControl().setText("");          }     } 
