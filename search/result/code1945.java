	 */ @Override  public String modifyString(String text, Setting setting) {  final StringBuilder sb = new StringBuilder(text);  final int maxLineLength = 80;  int i = 0;  // TODO check line breaks  while (i + maxLineLength < sb.length()) {  i = sb.lastIndexOf("\n", i + maxLineLength);   if (i == -1) {  i = 0; 			}  i = sb.lastIndexOf(" ", i + maxLineLength);   int multiplicator = 2;  while (i == -1) {  i = sb.lastIndexOf(" ", i + multiplicator * maxLineLength);   multiplicator++;  if (multiplicator * maxLineLength > sb.length()) {  break; 				} 			} } } 
