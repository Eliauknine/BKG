 			} catch (final IOException e) { Logger.error(Activator.PLUGIN_ID, "Cannot read class list for code completion", e); 			}  // read eclipse classes  final BundleContext context = FrameworkUtil.getBundle(JavaClassCompletionProvider.class).getBundleContext();  for (final Bundle bundle : context.getBundles()) {  final Collection<String> exportedPackages = JavaPackagesCompletionProvider.getExportedPackages(bundle);  // first look for class signatures in manifest, so we do not need to parse the whole bundle }
