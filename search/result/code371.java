 public void init(EObject modelElement) { // Initialize the animated diagrams manager. The initialize process consist in:  // 1 - Identifying diagrams located in model  // 2 - Build cache linking model element to list of diagrams in which it appears  if (modelElement instanceof Element) {  // Find all diagrams available in this model  Element owner = ((Element)modelElement).getOwner();  while(owner.getOwner() != null) {  owner = owner.getOwner(); 			}  Job diagramsLoading = new InitiliazeDiagramManagerJob(owner.getModel());  diagramsLoading.schedule();  try {  diagramsLoading.join(); } } } 
