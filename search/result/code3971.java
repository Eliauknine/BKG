 public void cdoInternalPostAttach(){  EClass eClass = eClass(); for (int i = 0; i < eClass.getFeatureCount(); i++)       {  Object setting = eSettings[i];  // if (setting != null)         {  EStructuralFeature eFeature = cdoInternalDynamicFeature(i);  if (!eFeature.isTransient())           {  populateRevisionFeature(view, revision, eFeature, eSettings, i);           }         }       }     } 
