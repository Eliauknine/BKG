 public void draw(GC gc, GalleryItem item, int index, int x, int y, int width, in{  // Calculate font height (text and description)  gc.setFont(textFont);  String text = createLabel(item.getText(), gc, width - useableHeight - 10);  int textFontHeight = gc.getFontMetrics().getHeight();  String description = null;  int descriptionFontHeight = 0;  if (item.getDescription() != null) {  gc.setFont(descriptionFont);  description = createLabel(item.getDescription(), gc, width - useableHeight - 10);  descriptionFontHeight = gc.getFontMetrics().getHeight(); 			} } 
