 /**	 * @param files 	 *            ordered by corresponding {@link IProject} 	 * @return Creates {@link Change}s for each {@link IFile} using {@link ImportOrganizer} 	 */  public List<Change> organizeImports(Multimap<IProject, IFile> files) {  List<Change> result = Lists.newArrayList();  for (IProject project : files.keySet()) {  ResourceSet resSet = resSetProvider.get(project);  for (IFile file : files.get(project)) {  URI uri = storageMapper.getUri(file);  if (uri != null) {  XtextResource resource = (XtextResource) resSet.getResource(uri, true); } } } } 
