   * Update the ActivityExplorer Editor.   * @param selectedViewpoint    */  protected void update(Viewpoint selectedViewpoint) {  final Session currentSession = SessionManager.INSTANCE.getSession(selectedViewpoint);  if ((selectedViewpoint != null) && (currentSession != null) && currentSession.isOpen()) {  Runnable refresh = new Runnable() {  @Override  public void run() {  ActivityExplorerEditor editor = ActivityExplorerManager.INSTANCE.getEditorFromSession(currentSession);  if (editor != null) {  editor.updateEditorPages(0);           }         }       };  run(refresh);     }   }  public void notifyAddSession(Session newSession) { } 
