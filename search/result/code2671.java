 private IMainPageForm createMainPageForm(IPage page) throws ProcessingException{     } else {  config.setKeepSelection(true);     }  return new MainPageForm(page, this, config);   }  private IPageForm createPageForm(IPage page) throws ProcessingException {  PageFormConfig config = new PageFormConfig();  config.setDetailFormVisible(true);  return new PageForm(page, this, config);   } 
