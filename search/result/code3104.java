 public static Test suite(){  @Override protected void initConfigSuites(TestSuite parent)   {  // addScenario(parent, COMBINED, MEM, JVM, NATIVE);  // addScenario(parent, COMBINED, MEM_AUDITS, JVM, NATIVE);  addScenario(parent, COMBINED, MEM_BRANCHES, JVM, NATIVE);   } }  @Override  protected void initConfigSuites(TestSuite parent)   {  // addScenario(parent, COMBINED, MEM, JVM, NATIVE);  // addScenario(parent, COMBINED, MEM_AUDITS, JVM, NATIVE);  addScenario(parent, SEPARATED, MEM_BRANCHES, SSL, NATIVE);   } 
