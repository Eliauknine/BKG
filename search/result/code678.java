 public void notifyWriteAccessHandlers(ITransaction transaction, CommitContext co{   {  if (beforeCommit && commitContext.getNewPackageUnits().length != 0)     {  throw new IllegalStateException(  "Synchronizable repositories don't support dynamic addition of new packages. Use IRepository.setInitialPackages() instead.");     }  super.notifyWriteAccessHandlers(transaction, commitContext, beforeCommit, monitor); } } 
